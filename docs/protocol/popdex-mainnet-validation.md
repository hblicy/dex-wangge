# PopDEX Mainnet Protocol Validation

Validated at: 2026-08-17T05:46:05.598Z

Official app artifact hashes:

| Path | SHA-256 | Bytes |
|---|---|---:|
| `/cdnstatic/libs/react.v18.3.0.min.js` | `6d749d1fc1ef6bab690553c2d9b8ce87a4bf070652c94f6a5ac91edd09631a14` | 10,737 |
| `/cdnstatic/libs/react-dom.v18.3.0.min.js` | `55567344f279961e4cd2ef7a8f00655a1fe3d0c01a5778c1db8686bad0f00c2f` | 131,835 |
| `/perp-static/client/js/runtime.d106d31259ba7b6290a8.main.js` | `ef6160b3685ff74993e6c87e0ccae06bef36e0ee3b614e7633d9bec291b5b0c2` | 71,835 |
| `/perp-static/client/js/55414.593cd675457744c6280f.main.js` | `367429c4c88ed1dc7113b1ab97b64cca6c91653ae44382dda1cdeef7a60a1fef` | 350,017 |
| `/perp-static/client/js/78432.585555fab022e620efa5.main.js` | `22d7fa6d72829ddf9e65a31aea94bcb6eadaffda90b14978971662542e0d0a9e` | 218,095 |
| `/perp-static/client/js/3810.c02285bc335fce0507df.main.js` | `57d31c54db4af61d00434510a0042d9b7af48d0e22c29e69e36bee8230cd2f8c` | 134,638 |
| `/perp-static/client/js/36948.5f4cab237bb8a5142f1a.main.js` | `bab395b811842b337cb4c7ed56aeeab6b9ac37d47cbe28491b80449097515566` | 221,889 |
| `/perp-static/client/js/89700.377cbce4b1cdd9bbfa15.main.js` | `a1c865f9c17dec910b8c178bd9bf8e7430b7b898d59bc85b460bba18ce1165d2` | 65,775 |
| `/perp-static/client/js/52912.e460f20673d1bde735e9.main.js` | `034985798b58ac3824162bb937904f12502de29ff1bbc0e237b7e3399a2c6073` | 50,989 |
| `/perp-static/client/js/client.663df906e64b0b2c3960.main.js` | `0c5306878e9ea4e7fc3746c1cc3266edb2ef45c7e9d2d50813078f8b03a9dddb` | 350,188 |
| `/perp-static/client/js/89374.b26847c430d8ded40742.chunk.js` | `2a57ecb06e8f4d3e5c8c8a2d44a5134c94ca4492bb2953f498fe5f13834f60a6` | 70,758 |
| `/perp-static/client/js/23502.c5265aff623be3f5b03f.chunk.js` | `7dd7a57c7bf8b52a3c161a657b8f75b032b977aff3c94973a5cc0fe76c293b36` | 54,035 |
| `/perp-static/client/js/91290.d4c92d838bb9f2c45546.chunk.js` | `917f4a7153e978454954ede2c4212f528a669f59fb70cbfea8fd095e772711b2` | 89,575 |
| `/perp-static/client/js/61535.fdc966582dba06e48cbd.chunk.js` | `21d247974bf6cf5a0345c2cfdd26d9eb03e3bd93a95160d1a79defc16d4f2e4f` | 40,302 |
| `/perp-static/client/js/5940.f73fd560528897e55f73.chunk.js` | `798a6ea03db64c0140870ea818cd603792e195d365d9eca42f89458c35ebc667` | 54,750 |
| `/perp-static/client/js/27283.bfbac9aa20eb0221e480.chunk.js` | `eb969147ce283cff5d66549ae0cb8861731f13f3aac023800de158f0ec74a793` | 227,411 |
| `/perp-static/client/js/1458.7f6170ce6b94c232fe65.chunk.js` | `eff99678a760357e7474ff9b5122378ba921ed1295bb6215448a45e4a024e613` | 48,147 |
| `/perp-static/client/js/64927.17a2965fd7f76c994173.chunk.js` | `852b784570bcfb54ac91ae7d8c70f7b14462c3d498c400b890cb539d08e35603` | 29,158 |
| `/perp-static/client/js/73758.eac12733832d57ef5f71.chunk.js` | `39be7618f89655194bfba81b6304b17b82466b8a499950650df383f000f8786d` | 44,096 |
| `/perp-static/client/js/5690.9031c89a0f4ac4a3ddb5.chunk.js` | `95c9759a26520569d8729212e799d13510ed37ee2211ddcccade72cf43fd0b93` | 172,216 |
| `/perp-static/client/js/13371.f010492037b1366100bc.chunk.js` | `f56a0a44daab44817adda1559fea34ecce71c0281bf8efacdfb64f750624acf3` | 59,208 |
| `/perp-static/client/js/9167.3027f10fe5dc7a333098.chunk.js` | `e42d7caafc7577afa3714037d3eec452f4b97a42c208f81e3d9dccdd637a9586` | 81,840 |
| `/perp-static/client/js/98039.36abf38e738893c0cb67.chunk.js` | `eb04dddebc56bba2fe8320c1af6becbcdc8412ef7a02d5f3a2fe8f83dd8ab813` | 110,155 |
| `/perp-static/client/js/9475.cfe0f71ceade09b28bb2.chunk.js` | `c9fc513ccb06bc6fbdc5a59a1c574235e200e179f7c5f27b5f4bdf1088310e53` | 48,852 |
| `/perp-static/client/js/44894.391331a4a8a8b6120e57.chunk.js` | `943912cc782fb4133277706389ab797b4012104901af156ee5a7d00db97aed3e` | 43,517 |
| `/perp-static/client/js/screens-Main.3500ad687658add27232.chunk.js` | `ecda72c7a69ef1bc2744e316f6a187ad1c1ddfa5398bf9152d18be2f567e7c6d` | 78,753 |
| `/perp-static/client/js/web3.aad0cd372ea71a17109e.chunk.js` | `92263dbf563db649a01c790bafdf014b13b8fdceeec3d3c08fa9bb2fd3c5c045` | 475 |

## Public identity

The official RPC at `https://app.popdex.xyz/api/v1/web3/rpc` returned `eth_chainId=0x888`, which is decimal `2184`. The verifier's RPC boundary rejected every non-read method and reported `writeMethodsCalled=0`.

The official `GET https://api.popdex.xyz/api/v1/config/symbols?category=Futures` response matched the two-market whitelist exactly:

| Market | symbolId | status | tickSize | lotSize | minQty | minNotional | defaultLeverage |
|---|---:|---|---:|---:|---:|---:|---:|
| BTCUSDT | 20000 | Trading | 1 | 0.0001 | 0.0001 | 10 | 20 |
| ETHUSDT | 20001 | Trading | 0.1 | 0.001 | 0.001 | 10 | 20 |

At the validation time, BTCUSDT returned bid `62965`, ask `62966`, last `62966`, index `62993`, and mark `62966`. ETHUSDT returned bid `1878.5`, ask `1878.6`, last `1878.7`, index `1879.3`, and mark `1878.6`. Both markets returned 200 candles using the observed seven-string row schema `[time, open, high, low, close, volume, notional]`.

## Account read model

The following requests were sent without `Authorization` or `dy-token`, using only `Accept`, `website`, `terminaltype`, `language`, `locale`, and `enterPointSource` headers:

| Endpoint | Observed result for a synthetic non-account address | Pagination evidence |
|---|---|---|
| `GET https://api.popdex.xyz/api/v1/account/{wallet}/orders?limit=2&symbol=BTCUSDT` | HTTP 200, `code="200"`, `data=[]` | No cursor was returned for the empty result. |
| `GET https://api.popdex.xyz/api/v1/account/{wallet}/trade/fills?limit=2&symbol=BTCUSDT` | HTTP 200, `code="200"`, `data=[]` | Top-level `cursor=""`, `limit="2"`, and `total="0"`. |
| `GET https://app.popdex.xyz/web/v1/account/{wallet}/overview` | HTTP 200, `code="11100"`, `msg="Account does not exist"`, `data=null` | Not applicable. |
| `eth_call` Order precompile `getOpenPositionsByAccount(account,0,100)` | Official ABI decoded successfully; a synthetic address returned `positions=[]`, `hasMore=false`. | Offset/limit pagination is explicit in the ABI. |

The corrected verifier completed a real `POPDEX_MAIN_ACCOUNT` run for masked account `0xdA1e…b3e8`: BTCUSDT returned `open=0`, `fills=0`; ETHUSDT returned `open=0`, `fills=0`; the Order precompile returned `positions=0`; and both verification phases reported `writeMethodsCalled=0`. This proves that the wallet-scoped REST endpoints, overview endpoint, and official on-chain position read are accessible without an Agent key or copied browser token. Official artifact `/perp-static/client/js/98039.36abf38e738893c0cb67.chunk.js` exposes `getOpenPositionsByAccount(address,uint32,uint32)` on the Order precompile. The verifier uses that read-only contract method and preserves every returned integer as a string. Non-empty order, fill and position rows and non-empty pagination semantics still require live evidence. The verifier does not substitute an empty account for the explicit `11100` error.

## Agent authorization

Artifact `/perp-static/client/js/98039.36abf38e738893c0cb67.chunk.js` identifies Account precompile `0x0000000000000000000000000000000000001008`. Its ABI includes:

```text
approveAgent(address agent,address delegator,bytes32 name,uint64 expiresAt,uint64 initialNonce,bool isGlobal)
replaceAgent(address oldAgent,address newAgent,uint64 expiresAt,uint64 initialNonce)
revokeAgent(address agent)
getAgent(address agent)
getAgents(address delegator)
```

Artifact `/perp-static/client/js/9475.cfe0f71ceade09b28bb2.chunk.js` shows the official UI generating a random 32-byte Agent private key locally and deriving its address. It constructs `initialNonce=BigInt(Math.floor(Date.now()/1e3))`, `expiresAt=BigInt(Date.now()+2592e6)`, a bytes32 name made from the `UI_` prefix and the runtime hostname, and `isGlobal=false`. The connected main wallet sends `approveAgent` or `replaceAgent` as a legacy transaction to the Account precompile. The UI waits for a successful receipt, reads `getAgent`, requires `exists=true`, and checks that the returned delegator equals the connected main account before treating the Agent as authorized.

Artifact `/perp-static/client/js/screens-Main.3500ad687658add27232.chunk.js` stores the generated Agent key under an account-scoped browser record whose key is the `bit_agent_` prefix followed by the account address, and encrypts the private key before writing it to local storage. This browser persistence mechanism is evidence of the official UI behavior, not authorization to copy the same storage design into the VPS service.

## Trading execution

Artifact `/perp-static/client/js/98039.36abf38e738893c0cb67.chunk.js` identifies Order precompile `0x0000000000000000000000000000000000001000` and exposes these relevant ABI functions:

```text
placeOrder(address account,bytes32 clientOrderId,uint16 symbolId,bytes32 orderParams,uint256 price,uint256 qty,uint256 slippage,address builder,uint256 builderFeeRate)
cancelOrder(address account,uint128 orderId,bytes32 clientOrderId)
placeReverseOrder(address account,uint16 symbolId,uint8 positionSide)
```

UserConfig precompile is `0x0000000000000000000000000000000000001009`. Its relevant ABI is:

```text
getAccountConfig(address account)
updateLeverage(address account,(uint8 newLeverage,uint16 symbolId,address tokenAddress,uint8 category) request)
LeverageUpdated(address account,uint8 category,uint16 symbolId,address tokenAddress,uint8 newLeverage,bool succeeded,uint32 code)
```

BTCUSDT leverage updates use `newLeverage=1`, `symbolId=20000`, zero `tokenAddress`, and `category=Futures(2)`. `OrderCategory.Regular(0)` is a different enum and must not be used as the leverage category. The Order precompile `0x0000000000000000000000000000000000001000` remains the source of `placeOrder`, `cancelOrder`, order pages, and position pages. Its ABI also declares `placeReverseOrder`, but the failed Mainnet transaction described below proves that it is not a valid substitute for the verified final-close order.

Artifact `/perp-static/client/js/screens-Main.3500ad687658add27232.chunk.js` proves at least one Agent write path: `updatePositionMode` is sent with the Agent account, `type="legacy"`, `nonce=Date.now()`, `gas=1000000`, and `gasPrice=0`, followed by receipt confirmation.

The captured official app also routes order actions through authenticated REST paths including `/web/v1/uta/order/place`, `/web/v1/uta/order/closePlace`, and `/web/v1/uta/order/closeAll`. Static evidence alone did not connect those REST calls to the local Agent's exact calldata, nonce, raw transaction sender, or receipt. The later bounded Mainnet probe supplied the missing evidence for one direct Agent `placeOrder` and `cancelOrder` flow; it does not prove the separate authenticated REST write routes or any other order action.

## Order identity

The Order precompile ABI proves that placement accepts a caller-provided `bytes32 clientOrderId`; cancellation requires both `uint128 orderId` and `bytes32 clientOrderId`; on-chain order getters expose both fields. Mainnet preflight evidence proves that the precompile parses this field as a restricted UTF-8 identifier: an arbitrary Keccak digest was rejected with `invalid UTF-8 in client order ID`, and a valid UTF-8 Base64URL label was rejected at its `_` character. The reference implementation uses `encodeBytes32String` with letters, digits and hyphens. New probe IDs therefore use the fixed `dw-<market><side>-` prefix plus 96 bits of lowercase hexadecimal randomness, remain within 31 UTF-8 bytes, and use canonical bytes32 zero padding. All verifier parsers preserve IDs and cursors as strings.

Mainnet `eth_call` evidence also corrects the write return ABI: a valid `placeOrder` simulation returned exact empty data `0x`, while rejected inputs returned explicit JSON-RPC errors from the precompile. Therefore `placeOrder` and `cancelOrder` have no decodable bool result. The probe accepts only exact `0x` as a successful simulation and still rejects every RPC error or non-empty result before signing.

The current hashed artifact `/perp-static/client/js/98039.36abf38e738893c0cb67.chunk.js` also exposes both authoritative order-page reads:

```text
getActiveOrdersByAccount(address account,uint32 offset,uint32 limit) -> OrdersPage
getCompletedOrdersByAccount(address account,uint32 offset,uint32 limit) -> OrdersPage
```

`OrdersPage` contains `OrderInfo[] orders` and `bool hasMore`. Each `OrderInfo` contains `walletId`, `category`, `source`, `orderId`, `clientOrderId`, `symbolId`, `side`, `isReduceOnly`, `orderType`, `timeInForce`, `stpMode`, `stpKey`, `status`, `price`, quantity fields (`qty`, `filledQty`, `remainingQty`, `cancelledQty` and their quote variants), `averagePrice`, `nonce`, timestamps and fee rates. Mainnet evidence showed that both precompile order pages can remain empty while the official REST endpoint and web UI show a live `NewAccept` order, and that the REST order endpoint removes the row after cancellation instead of returning a `Cancelled` row. The bounded probe therefore anchors cancellation on the exact successful transaction receipt and matching `OrderCancel` event, then requires absence from the REST open-order set, zero matching fills across bounded REST pagination, and no target-market position across bounded on-chain pagination before clearing recovery state.

The approved 2026-08-17 minimum-value write probe placed BTCUSDT order `234237619377012736` through the authorized Agent. The web UI and official REST exposed the live order. Agent cancellation produced a successful receipt and exact `OrderCancel`; subsequent verification returned BTCUSDT `open=0`, `fills=0`, ETHUSDT `open=0`, `fills=0`, and `positions=0`. This establishes the observed cancellation disappearance semantics used by recovery. It does not authorize automated grid trading, marketable fills, leverage changes, or position closing.

The same authoritative ABI exposes `getOpenPositionsByAccount(address account,uint32 offset,uint32 limit)` and returns `PositionInfo[] positions` plus `bool hasMore`. `PositionInfo` contains `walletId`, `positionId`, `symbolId`, `side`, `holdSize`, `avgOpenPrice`, `closeSize`, `lockedSize`, `realizedPnl`, `createdTime`, and `updatedTime`. A Mainnet read-only call against precompile `0x0000000000000000000000000000000000001000` decoded successfully. No REST position array is inferred from `/overview`.

The bounded Mainnet probe established the mapping through the exact `OrderCreate` event in the recorded placement receipt and the matching official REST row. The implementation still rejects treating `txHash` itself as `orderId`.

## Stage 5 fill-close probe

The 2026-08-18 Mainnet probe successfully set and read back BTCUSDT 1x leverage, created a minimum Buy/Long fill, normalized its `execId`, and confirmed the resulting OneWay long position. The probe's first close transaction `0x3e921b685c9268015c152637d2c961abf3850449cbc9a5fefa5955474175bbda` called `placeReverseOrder(account,20000,Long=1)`, failed with `[15200] Invalid position side`, and left the position open. This disproves the previous inference that the read-side position value `Long=1` is a valid write-side close parameter. That primitive is rejected for this workflow and must not be retried.

The subsequently captured successful official UI close transaction `0xbb8f28e0cbef74faa8d706676ec6ad93c89196d6440cc4ac8d46c362c8b91e50` used Order precompile `0x0000000000000000000000000000000000001000`, selector `placeOrder`, and a Market Sell order with ReduceOnly and Net position mode. Its verified values were `price=0`, quantity equal to the exact open-position WAD amount, `slippage=3%`, zero builder, and zero builder fee. This transaction is the protocol source of truth for the corrected close encoder. It does not change ordinary grid order semantics: future grid placement and replenishment remain Limit + GTC.

The corrected isolated CLI implementation now has an offline-tested journal-v2 recovery path: it persists a distinct close client ID, exact close quantity, deterministic transaction hash and receipt-derived order ID; then confirms the matching Sell fills, zero active orders and final zero positions before completion. The default command and ordinary `--resume` remain read-only. `--confirm-mainnet-fill-close`, `--resume --confirm-mainnet-cancel`, and `--resume --confirm-mainnet-close` are separate explicit write gates and never authorize an automatic retry after restart. A failed legacy close receipt can be cleared only after authoritative read-only facts prove zero orders and zero positions.

The 2026-08-18 Mainnet fill response was observed to expose its exact decimal-string identity as `execId`, with no `fillId` or `tradeId` field. The account normalizer maps that verified field to the internal `fillId` while rejecting conflicting aliases, so fill deduplication and recovery retain the exchange-provided identity without numeric conversion.

Automated tests cover the state machine, but mocked reverse-close success is not Mainnet protocol evidence. Mainnet has now validated the leverage write, marketable fill schema and non-empty position schema; it has rejected the assumed reverse-close parameter. A failed close record can be cleared after an external/manual close only when read-only facts prove the account is flat, without broadcasting another transaction.

## Required write probes

The first bounded probe is complete: a newly authorized Agent was verified on chain, one non-marketable minimum-value BTCUSDT order was signed and broadcast by that Agent, the exact `OrderCreate` receipt mapped the deterministic `clientOrderId` to the official `orderId`, REST and the web UI confirmed the live order, and a single Agent cancellation produced the exact `OrderCancel` receipt. The official UI close transaction has also supplied the exact final-close calldata described above. The corrected Stage 5 Agent implementation has passed offline state-machine and regression tests only. The remaining separately approved probes are:

1. Execute one bounded BTCUSDT Stage 5 Agent fill-close validation using the corrected reduce-only market close, then confirm the exact close order, fill total, zero active orders and zero positions. Do not repeat the rejected `placeReverseOrder` transaction.
2. Record the official maximum-open-order response and bounded rate-limit headers or errors; no current authoritative limit was found in the captured evidence.

## Conclusion

BLOCKED

| Protocol gate | Result |
|---|---|
| Mainnet chain ID and RPC identity | Validated: chain ID 2184. |
| Account precompile, Agent ABI, nonce units and readback | Validated from hashed official artifacts. |
| Order precompile ABI and Agent signing/send model | Validated for one non-marketable place and cancel probe. |
| Agent, main account and delegated order relationship | Validated for the bounded place/cancel probe through on-chain Agent readback, signed transactions and exact receipt events. |
| `txHash`, `clientOrderId`, `orderId` mapping | Validated for the bounded probe through the exact `OrderCreate` receipt and REST order identity. |
| Place, cancel, leverage and fill states | Validated for one non-marketable place/cancel and one minimum BTCUSDT marketable fill. The rejected reverse close failed with `[15200]`; the official UI's successful reduce-only market close is decoded and offline-tested, while one bounded Bot-Agent close validation remains pending. |
| Complete order, fill and position schema and pagination | Partially validated: real empty and non-empty BTCUSDT facts were observed, including `execId` and a OneWay long position. Pagination beyond the bounded rows remains blocked. |
| Maximum open orders and rate limits | Blocked: no authoritative value was present in the read-only evidence. |

The next protocol action is one separately approved, bounded Bot-Agent validation of the corrected Stage 5 close. The official UI transaction already proves the close primitive and final flat state; the remaining validation must prove the local Agent implementation's exact close order and fill identities without adding server routes or automatic grid trading. PopDEX automatic grid trading remains disabled until Stage 5 is accepted and the later `IExchange` stage is separately designed and implemented.
